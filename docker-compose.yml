services:
  official-doc-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: doc-coordination-system
    expose:
      - "5000"
    ports:
      - "59607:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - DB_PATH=/app/data/documents.db
      - OcrSettings__TessDataPath=/usr/share/tesseract-ocr/5/tessdata
      - TESSDATA_PREFIX=/usr/share/tesseract-ocr/5/tessdata
      - TZ=Asia/Ho_Chi_Minh
      - JWT_SECRET=${JWT_SECRET}
      - PUBLIC_ID_SECRET=${PUBLIC_ID_SECRET}
      - VAPID_SUBJECT=mailto:admin@linkstrategy.vn
    volumes:
      - ./data_dump:/app/data
      - ./ToolCalendar.Api/Uploads:/app/Uploads
    restart: always

  nginx:
    image: nginx:alpine
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    depends_on:
      - official-doc-backend
    restart: always

  ngrok:
    image: ngrok/ngrok:alpine
    container_name: doc-coordination-ngrok
    restart: always
    environment:
      - NGROK_AUTHTOKEN=NGROK_TOKEN_REDACTED
    command:
      - http
      - nginx-proxy:80
      - --domain=NGROK_DOMAIN_REDACTED
    depends_on:
      - nginx
